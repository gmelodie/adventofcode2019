import sys
import numpy as np


if __name__ == "__main__":
    maze = []
    for line in sys.stdin:
        maze.append(line.strip('\n'))
    maze = np.array(maze)
    print(maze)

    find_pos('AA')
